---
import Logo from "./Logo.astro";
import Button from "@/components/ui/Button.astro";
import { docsNav, guideNav } from "@/lib/nav";

const simpleLinks = [
  { href: "/", label: "Home" },
  { href: "/features", label: "Features" },
  { href: "/pricing", label: "Pricing" },
  { href: "/savings", label: "Savings" },
];

const path = Astro.url.pathname.replace(/\/$/, "") || "/";
const isActive = (href: string) => (href === "/" ? path === "/" : path === href || path.startsWith(href + "/"));
const AUTH_URL = "https://login.quickztna.com/auth";

const triggerBase =
  "inline-flex items-center gap-1 rounded-md px-3 py-2 text-sm font-medium transition-colors hover:bg-muted hover:text-foreground";
---
<header class="sticky top-0 z-40 w-full border-b border-border/60 bg-background/80 backdrop-blur-md">
  <div class="mx-auto flex h-16 max-w-7xl items-center justify-between px-4 sm:px-6 lg:px-8">
    <a href="/" class="flex items-center">
      <Logo />
    </a>

    <!-- Desktop nav -->
    <nav class="hidden items-center gap-1 md:flex">
      {simpleLinks.map((l) => (
        <a
          href={l.href}
          class={`rounded-md px-3 py-2 text-sm font-medium transition-colors hover:bg-muted hover:text-foreground ${
            isActive(l.href) ? "text-foreground bg-muted" : "text-muted-foreground"
          }`}
        >
          {l.label}
        </a>
      ))}

      <!-- Docs dropdown (categories) -->
      <div class="group relative">
        <button
          type="button"
          aria-haspopup="menu"
          class={`${triggerBase} ${isActive("/docs") ? "text-foreground bg-muted" : "text-muted-foreground"}`}
        >
          Docs
          <svg data-chev class="h-3 w-3 transition-transform group-hover:rotate-180" viewBox="0 0 12 12" fill="none" aria-hidden="true"><path d="M3 4.5 6 7.5 9 4.5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
        </button>
        <div class="invisible absolute left-0 top-full z-50 pt-2 opacity-0 transition-opacity duration-150 group-hover:visible group-hover:opacity-100 group-focus-within:visible group-focus-within:opacity-100">
          <div class="w-72 rounded-lg border border-border bg-background p-2 shadow-lg">
            {docsNav.map((g) => (
              <a href={g.items[0].href} class="block rounded-md px-3 py-2 transition-colors hover:bg-muted">
                <span class="block text-sm font-medium text-foreground">{g.title}</span>
                <span class="block text-xs text-muted-foreground">{g.items[0].label}</span>
              </a>
            ))}
          </div>
        </div>
      </div>

      <!-- Guide dropdown (grouped mega-panel) -->
      <div class="group relative">
        <button
          type="button"
          aria-haspopup="menu"
          class={`${triggerBase} ${isActive("/guide") ? "text-foreground bg-muted" : "text-muted-foreground"}`}
        >
          Guide
          <svg data-chev class="h-3 w-3 transition-transform group-hover:rotate-180" viewBox="0 0 12 12" fill="none" aria-hidden="true"><path d="M3 4.5 6 7.5 9 4.5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
        </button>
        <div class="invisible absolute right-0 top-full z-50 pt-2 opacity-0 transition-opacity duration-150 group-hover:visible group-hover:opacity-100 group-focus-within:visible group-focus-within:opacity-100">
          <div class="grid w-[34rem] max-w-[calc(100vw-2rem)] grid-cols-2 gap-x-6 gap-y-4 rounded-lg border border-border bg-background p-4 shadow-lg">
            {guideNav.map((g) => (
              <div>
                <p class="mb-1.5 font-mono text-[10px] font-semibold uppercase tracking-wider text-muted-foreground">{g.title}</p>
                <ul class="space-y-0.5">
                  {g.items.map((it) => (
                    <li>
                      <a href={it.href} class="block rounded px-2 py-1 text-sm text-muted-foreground transition-colors hover:bg-muted hover:text-foreground">{it.label}</a>
                    </li>
                  ))}
                </ul>
              </div>
            ))}
          </div>
        </div>
      </div>

      <a
        href="/blog"
        class={`rounded-md px-3 py-2 text-sm font-medium transition-colors hover:bg-muted hover:text-foreground ${
          isActive("/blog") ? "text-foreground bg-muted" : "text-muted-foreground"
        }`}
      >
        Blog
      </a>
    </nav>

    <div class="flex items-center gap-2">
      <Button variant="ghost" size="sm" href={AUTH_URL} class="hidden sm:inline-flex">Sign in</Button>
      <Button size="sm" href={AUTH_URL} class="font-medium">Get started free</Button>
      <!-- Mobile hamburger -->
      <button
        id="mobile-nav-toggle"
        type="button"
        aria-label="Toggle navigation menu"
        aria-expanded="false"
        aria-controls="mobile-nav"
        class="inline-flex items-center justify-center rounded-md p-2 text-muted-foreground transition-colors hover:bg-muted hover:text-foreground md:hidden"
      >
        <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" aria-hidden="true"><path d="M4 6h16M4 12h16M4 18h16" stroke="currentColor" stroke-width="2" stroke-linecap="round"/></svg>
      </button>
    </div>
  </div>

  <!-- Mobile drawer -->
  <div id="mobile-nav" class="hidden border-t border-border/60 bg-background md:hidden">
    <nav class="mx-auto max-w-7xl space-y-1 px-4 py-3 text-sm sm:px-6">
      {simpleLinks.map((l) => (
        <a href={l.href} class="block rounded-md px-3 py-2 font-medium text-muted-foreground hover:bg-muted hover:text-foreground">{l.label}</a>
      ))}

      <!-- Docs (collapsible) -->
      <button type="button" data-section="m-docs" class="flex w-full items-center justify-between rounded-md px-3 py-2 font-medium text-muted-foreground hover:bg-muted hover:text-foreground">
        <span>Docs</span>
        <svg data-chevron class="h-3.5 w-3.5 transition-transform" viewBox="0 0 12 12" fill="none" aria-hidden="true"><path d="M3 4.5 6 7.5 9 4.5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
      </button>
      <div id="m-docs" class="hidden space-y-0.5 border-l border-border/60 pl-3">
        {docsNav.map((g) => (
          <a href={g.items[0].href} class="block rounded px-3 py-1.5 text-muted-foreground hover:bg-muted hover:text-foreground">{g.title}</a>
        ))}
      </div>

      <!-- Guide (collapsible, grouped) -->
      <button type="button" data-section="m-guide" class="flex w-full items-center justify-between rounded-md px-3 py-2 font-medium text-muted-foreground hover:bg-muted hover:text-foreground">
        <span>Guide</span>
        <svg data-chevron class="h-3.5 w-3.5 transition-transform" viewBox="0 0 12 12" fill="none" aria-hidden="true"><path d="M3 4.5 6 7.5 9 4.5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
      </button>
      <div id="m-guide" class="hidden border-l border-border/60 pl-3">
        {guideNav.map((g) => (
          <div class="mb-2">
            <p class="px-3 py-1 font-mono text-[10px] font-semibold uppercase tracking-wider text-muted-foreground/70">{g.title}</p>
            {g.items.map((it) => (
              <a href={it.href} class="block rounded px-3 py-1.5 text-muted-foreground hover:bg-muted hover:text-foreground">{it.label}</a>
            ))}
          </div>
        ))}
      </div>

      <a href="/blog" class="block rounded-md px-3 py-2 font-medium text-muted-foreground hover:bg-muted hover:text-foreground">Blog</a>
    </nav>
  </div>
</header>

<script>
  function initNav() {
    const toggle = document.getElementById("mobile-nav-toggle");
    const menu = document.getElementById("mobile-nav");
    if (toggle && menu) {
      toggle.addEventListener("click", () => {
        const nowOpen = menu.classList.toggle("hidden") === false;
        toggle.setAttribute("aria-expanded", String(nowOpen));
      });
    }
    document.querySelectorAll<HTMLButtonElement>("[data-section]").forEach((btn) => {
      btn.addEventListener("click", () => {
        const id = btn.getAttribute("data-section");
        if (!id) return;
        document.getElementById(id)?.classList.toggle("hidden");
        btn.querySelector("[data-chevron]")?.classList.toggle("rotate-180");
      });
    });
  }
  initNav();
  document.addEventListener("astro:after-swap", initNav);
</script>
